# Machine Setup Dispatch System

## Context

Vectrik currently has no digital system for dispatching machine setups to operators. The scheduler has no way to tell CNC/EDM operators what to set up next, track setup progress, or optimize changeover sequences. Work orders create jobs and stage executions, but the bridge between "this execution is scheduled" and "operator, go set up Machine X with Program Y" doesn't exist.

This system closes that gap with an intelligent dispatch engine that balances three competing goals: minimize changeovers, meet due dates, and maximize throughput — with maintenance awareness and progressive learning built in.

## Architecture Overview

```
WorkOrders → Jobs → StageExecutions
                         ↓
              SetupDispatchService (Intelligence Engine)
                    ↓              ↓
         SetupDispatch        SetupHistory (immutable ledger)
           ↓        ↓              ↓
    Scheduler     Operator    LearningEngine
    Dispatch      Setup       (EMA setup times,
    Board         Queue        operator proficiency)
```

---

## Phase 1: Foundation (Core Data + Manual Dispatch)

### New Models

**`Models/SetupDispatch.cs`** — The atomic dispatch unit
- `DispatchNumber` (auto-generated, e.g. "DSP-00142")
- `MachineId`, `MachineProgramId?`, `StageExecutionId?`, `JobId?`, `PartId?` (denormalized for fast queries)
- `Status` — DispatchStatus enum: `Queued → Assigned → InProgress → PendingVerification → Verified → Completed` (also: Cancelled, Deferred, Blocked)
- `DispatchType` — enum: Setup, Changeover, Maintenance, BuildPlateLoad, Inspection, Teardown
- `Priority` (1-100 composite score), `PriorityReason` (human-readable), `ScoreBreakdownJson`
- `AssignedOperatorId?`, `RequestedByUserId?`
- `EstimatedSetupMinutes?`, `ActualSetupMinutes?`, `EstimatedChangeoverMinutes?`, `ActualChangeoverMinutes?`
- `ToolingRequired?`, `FixtureRequired?`, `WorkInstructionId?`
- `ScheduledStartAt?`, `QueuedAt`, `AssignedAt?`, `StartedAt?`, `VerifiedAt?`, `CompletedAt?`
- `ChangeoverFromProgramId?`, `ChangeoverToProgramId?`
- `PredecessorDispatchId?` (chain dispatches)
- `MaintenanceWorkOrderId?` (if type=Maintenance)
- `IsAutoGenerated`, `Notes?`

**`Models/SetupHistory.cs`** — Immutable ledger, feeds learning
- `SetupDispatchId`, `MachineId`, `MachineProgramId?`, `PartId?`, `OperatorUserId?`
- `SetupDurationMinutes`, `ChangeoverDurationMinutes?`, `WasChangeover`, `PreviousProgramId?`
- `ToolingUsedJson?`, `QualityResult?`, `CompletedAt`, `ShiftId?`

**`Models/OperatorSetupProfile.cs`** — Per-operator per-machine proficiency (EMA pattern from LearningService)
- `UserId`, `MachineId`, `MachineProgramId?`
- `AverageSetupMinutes?`, `SampleCount`, `VarianceMinutes?`, `FastestSetupMinutes?`
- `ProficiencyLevel` (1-5: Novice→Expert, auto-calculated)
- `IsPreferred` (admin flag)

**`Models/DispatchConfiguration.cs`** — Per-machine or global dispatch settings
- `MachineId?` (null = global default), `ProductionStageId?`
- `AutoDispatchEnabled`, `MaxQueueDepth` (default 3), `LookAheadHours` (default 8)
- `ChangeoverPenaltyWeight` (0.35), `DueDateWeight` (0.45), `ThroughputWeight` (0.20)
- `MaintenanceBufferHours` (default 4), `RequiresVerification` (default true)
- `AutoAssignOperator`, `NotifyOnDispatch`

### New Enums (add to `Models/Enums/ManufacturingEnums.cs`)
- `DispatchStatus`: Queued, Assigned, InProgress, PendingVerification, Verified, Completed, Cancelled, Deferred, Blocked
- `DispatchType`: Setup, Changeover, Maintenance, BuildPlateLoad, Inspection, Teardown
- `MachineSetupState`: Unknown, SetUp, ChangingOver, AwaitingSetup, Running, MaintenanceDue, MaintenanceInProgress

### Existing Model Changes
- **`Machine.cs`**: Add `CurrentProgramId?` (FK), `SetupState` (MachineSetupState), `LastSetupChangeAt?`
- **`StageExecution.cs`**: Add `SetupDispatchId?` (FK)
- **`MachineProgram.cs`**: Add `ActualAverageSetupMinutes?`, `SetupSampleCount`, `SetupVarianceMinutes?`

### Service: `ISetupDispatchService` / `SetupDispatchService`
Phase 1 scope: manual CRUD + lifecycle transitions (no scoring)
- `CreateManualDispatchAsync(request)` — scheduler creates dispatch
- `AssignOperatorAsync`, `StartDispatchAsync`, `CompleteDispatchAsync`, `CancelDispatchAsync`, `DeferDispatchAsync`
- `RequestVerificationAsync`, `VerifyDispatchAsync`
- `GetMachineQueueAsync(machineId)`, `GetOperatorQueueAsync(operatorUserId)`
- `GetDashboardDataAsync()`, `GetActiveDispatchesAsync()`

### SignalR: `Hubs/DispatchHub.cs` + `IDispatchNotifier`
- Groups: `dispatch:{tenant}`, `dispatch:{tenant}:machine:{id}`, `dispatch:{tenant}:operator:{id}`
- Events: DispatchCreated, DispatchStatusChanged, QueueReprioritized, UrgentDispatch

### UI: Basic Dispatch Board + Operator Queue

**Scheduler Dispatch Board** (`Components/Pages/Scheduler/Views/DispatchBoard.razor`)
- Machine swim lanes showing: machine name + status, current dispatch, queued dispatches as cards
- Manual "Create Dispatch" modal
- Unassigned work pool (scheduled StageExecutions without dispatches)

**Operator Setup Queue** (`Components/Pages/ShopFloor/SetupQueue.razor`)
- Tablet-optimized with large touch targets
- Current setup panel: program info, tooling checklist, elapsed timer, Complete/Pause
- Next up list: assigned dispatches with Accept button
- Barcode scan input for quick dispatch lookup

### Files to Create (Phase 1)
- `Models/SetupDispatch.cs`, `Models/SetupHistory.cs`, `Models/OperatorSetupProfile.cs`, `Models/DispatchConfiguration.cs`
- `Services/ISetupDispatchService.cs`, `Services/SetupDispatchService.cs`
- `Hubs/DispatchHub.cs`, `Hubs/IDispatchNotifier.cs`, `Hubs/DispatchNotifier.cs`
- `Components/Pages/Scheduler/Views/DispatchBoard.razor`
- `Components/Pages/Scheduler/Components/MachineLane.razor`, `DispatchCard.razor`, `UnassignedPool.razor`
- `Components/Pages/Scheduler/Modals/CreateDispatchModal.razor`
- `Components/Pages/ShopFloor/SetupQueue.razor`
- `Components/Pages/ShopFloor/Partials/CurrentSetup.razor`, `SetupQueueList.razor`
- EF Migration for new tables + field additions
- Register in `Program.cs` and `TenantDbContext.cs`

### Files to Modify (Phase 1)
- `Models/Machine.cs` — add CurrentProgramId, SetupState, LastSetupChangeAt
- `Models/MachineProgram.cs` — add setup EMA fields
- `Models/StageExecution.cs` — add SetupDispatchId
- `Models/Enums/ManufacturingEnums.cs` — add new enums
- `Data/TenantDbContext.cs` — register new DbSets
- `Program.cs` — register new services and hub
- `Components/Pages/Scheduler/Index.razor` — add DispatchBoard as a view option
- `Components/Layout/NavMenu.razor` — add Setup Queue nav link under Shop Floor

---

## Phase 2: Intelligence Engine

### Scoring Algorithm (composite 0-100)

**Due Date Score** (weight: configurable, default 0.45)
- Traces `StageExecution → Job → WorkOrderLine → WorkOrder.DueDate`
- Overdue: 100, within 8h: 95, within 24h: 80, within 48h: 60, within 5d: 40
- Priority multiplier: Emergency 1.5x, Rush 1.3x, High 1.1x, Normal 1.0x, Low 0.8x
- Critical path bonus: +15 if completing this unlocks 2+ downstream stages
- Defense contract bonus: +10

**Changeover Score** (weight: configurable, default 0.35)
- Same program as `Machine.CurrentProgramId`: 100 (no changeover)
- Same fixture, different program: 70
- Different fixture, same part family: 50
- Full changeover: `max(0, 100 - estimatedChangeoverMinutes * 2)`
- Changeover estimation priority: SetupHistory EMA → ProcessStage.SetupChangeoverMinutes → MachineProgram.SetupTimeMinutes

**Throughput Score** (weight: configurable, default 0.20)
- Count downstream waiting executions unlocked: `min(100, count * 15)`
- Batch fill bonus: +20 if grouping with adjacent same-program dispatches fills >= 80% capacity
- Machine idle bonus: +15 if machine currently idle

**Maintenance Modifier** (-50 to +20, applied after weighted sum)
- Job overruns maintenance window: -50
- Job fits comfortably: +10
- Machine in maintenance buffer window: +20 for short jobs
- Shutdown maintenance pending: -100 (block machine)

**Formula:** `final = clamp((dueDate * w1) + (changeover * w2) + (throughput * w3) + maintenanceMod, 0, 100)`

### Auto-Dispatch Generation
- `DispatchGenerationBackgroundService` (IHostedService)
- Controlled by `dispatch.auto_enabled` + per-machine `AutoDispatchEnabled`
- Default: auto-dispatches require scheduler approval

### Changeover Optimization
- Greedy nearest-neighbor sequencing by (ProgramId, Fixture)

---

## Phase 3: Maintenance Integration

- Maintenance modifier in scoring
- Auto-generate Maintenance-type dispatches
- Integrate with ProgramToolingItem wear tracking
- Maintenance alerts in both UI views

---

## Phase 4: Learning & Operator Proficiency

- Setup-specific EMA learning (extends LearningService pattern)
- Changeover-specific EMA (from→to program transitions)
- Operator proficiency: compare operator average to machine median (5 levels)
- Proficiency-based auto-assignment (opt-in)

---

## Phase 5: Configuration & Polish

- Admin settings page with weight sliders
- Per-machine configuration UI
- SLS BuildPlateLoad dispatch integration
- Historical analytics, notifications, seed data

### SystemSettings Keys
| Key | Default | Purpose |
|---|---|---|
| `dispatch.auto_enabled` | false | Master auto-dispatch switch |
| `dispatch.lookahead_hours` | 8 | Planning horizon |
| `dispatch.changeover_weight` | 0.35 | Changeover optimization weight |
| `dispatch.duedate_weight` | 0.45 | Due date urgency weight |
| `dispatch.throughput_weight` | 0.20 | Throughput maximization weight |
| `dispatch.maintenance_buffer_hours` | 4 | Short-job routing trigger |
| `dispatch.max_queue_depth` | 3 | Default max dispatches per machine |
| `dispatch.setup_ema_alpha` | 0.3 | EMA smoothing for setup times |
| `dispatch.batch_grouping_window_hours` | 4 | Batch same-setup jobs within window |
| `dispatch.require_scheduler_approval` | true | Auto dispatches need approval |

---

## Key Design Decisions

1. **SetupDispatch decoupled from StageExecution** — dispatches can be cancelled/deferred/batched without corrupting execution tracking
2. **Scoring weights are tenant-configurable** — high-mix shops prioritize changeover; defense shops prioritize due dates
3. **EMA learning reuses existing LearningService pattern** — same math, same configurable alpha
4. **Background auto-dispatch is opt-in** — manual first, build trust, enable per-machine gradually
5. **Separate DispatchHub** — different consumers/payloads than MachineStateHub
6. **SetupHistory is append-only** — clean training data + full audit trail
7. **Machine.CurrentProgramId tracks floor state** — bridges scheduler intent vs reality for accurate changeover scoring
