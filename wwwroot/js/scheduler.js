// scheduler.js - Gantt chart rendering for production scheduler
// Placeholder for future Gantt chart implementation.
// Current scheduler uses Blazor table rendering.
window.Scheduler = {
    init: function (containerId) {
        console.log('Scheduler initialized for', containerId);
    }
};
