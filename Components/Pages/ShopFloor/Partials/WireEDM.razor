@* Wire EDM - Rich built-in stage partial *@
@using Opcentrix_V3.Models

<div class="card" style="margin-bottom: 16px;">
    <div class="card-header">⚡ Wire EDM Details</div>
    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 12px;">
        <div class="form-group"><label class="form-label">Cut Duration (min)</label><input class="form-control" type="number" inputmode="numeric" /></div>
        <div class="form-group"><label class="form-label">Wire Used (m)</label><input class="form-control" type="number" step="0.1" inputmode="decimal" /></div>
        <div class="form-group"><label class="form-label">Wire Type</label>
            <select class="form-control"><option>Brass 0.25mm</option><option>Brass 0.20mm</option><option>Molybdenum</option></select>
        </div>
    </div>
</div>

@code {
    [Parameter] public StageExecution? Execution { get; set; }
}
